<%- include('./components/webHeader.ejs') %> <% if (productsLimit10.length) { %>
<h2 class="font-bold text-3xl leading-10 mt-4 text-center">Productos</h2>
<section
  class="mx-auto max-w-7xl container grid md:grid-cols-2 grid-cols-1 lg:grid-cols-4 gap-4 my-4 md:px-8 px-4"
>
  <% for( let product of productsLimit10 ) { %> <%-
  include('./components/product-card.ejs', {product}) %> <% } %>
</section>

<% } else { %>
<div>No hay productos</div>
<% } %>

<!-- <%- include('./components/card.ejs', {name: 'Robert', lastname: 'Principe'}) %> -->

<%- include('./components/webFooter.ejs') %>
